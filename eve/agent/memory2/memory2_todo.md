

Next:
- Create "get_old_memories" agent tool that retrieves old reflections from certain timewindow
- visualize session reflections in session settings UI
- fix ui bug for memories display (wider than screen)
- update memory prompts to force exact, absolute dates even more (avoiding cascading memory failures)
- check facts-deduplication flow using embeddings (user/agent scopes)
 
Optional:
- move consolidation word limits and other settings to db instead of hardcoded
- disable agent memory when user loses pro tier (run check in ui when loading agent and update flag)

