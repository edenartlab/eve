name: Combine audio and video
description: Combine audio and video
cost_estimate: 0.1
output_type: video
active: true
handler: local
visible: false
parameters:
  video:
    type: video
    label: Video
    description: Video URL
    required: true
  audio:
    type: array
    items:
      type: string
    label: Audio tracks
    description: Audio URLs to mix over the video
    tip: These are superimposed on top of each other, not sequential.
    required: true
