name: Search for Models
description: Search database for models
tip: Use this tool to search for agents based on a query. The model_id field is the unique ID for this model. If you wish to use this model, put its ID into the "lora" parameter of any model that can use Loras.
cost_estimate: 0
output_type: image
status: prod
visible: false
parameters:
  query:
    type: string
    label: Query
    description: Search query
    tip: Use this to describe in natural language which models you are searching for
    required: true
