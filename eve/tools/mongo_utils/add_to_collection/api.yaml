name: Add to Collection
description: Add a creation to a Collection that is in the database
tip: Use this tool to add a creation to a collection based on a search query.
cost_estimate: 0
output_type: image
status: prod
visible: false
parameters:
  query:
    type: string
    label: Query
    description: Search query
    tip: Describe in natural language which collection you are searching for to add the creation to
    required: true
  creation_id:
    type: string
    label: Creation ID
    description: The ID of the creation to add to the collection
    required: true
