name: abraham_publish
description: Abraham publish a new Creation
tip: |-
  This tool allows Abraham to publish a new Creation. Do not use this tool unless explicitly requested to use the abraham_publish tool.
cost_estimate: 0
output_type: string
active: true
handler: local
visible: false
parameters:
  title:
    type: string
    description: The publication title to post.
    required: true
  text:
    type: string
    description: The publication text content to post. The maximum length before the fold is 320 characters, and the maximum length is 1024 characters, so attempt to be concise.
    required: true
  media_urls:
    type: array
    description: Optional array of URLs to embed in the publication (images, links, etc.)
    required: false
    items:
      type: string
